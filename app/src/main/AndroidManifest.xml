<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ceyentra.restaurant">

    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplachScreen"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainForm" />
        <activity android:name=".Item01" />
        <activity android:name=".Item02" />
        <activity android:name=".Item03" />
        <activity android:name=".Item04" />
        <activity android:name=".Item05" />
        <activity android:name=".DialogForm" />
        <activity android:name=".PlaceOrder" />
        <activity android:name=".DialogTest"></activity>
    </application>

</manifest>